"use strict";

class Action {
  constructor (nameAction, date, start, end) {
    this.nameAction = nameAction;
    this.date = date;
    this.start = start;
    this.end = end;
  }
}

export default Action;